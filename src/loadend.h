#pragma once
#ifndef LOADEND_H_
#define LOADEND_H_
#include <fstream>
#include <iostream>
#include <string>

void GiveSentence(const int& day);
std::string LoadEndTxt(const int& day);

#endif // ! LOADTXT_H_
#pragma once
